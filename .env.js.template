/**
 * LOCAL ENVIRONMENT VARIABLES
 * --------------------------
 * This file contains local environment variables for development.
 * DO NOT commit this file to version control.
 * Rename this file to .env.js and add your actual values.
 */

window.env = window.env || {};

// Your GitHub personal access token
window.env.PAT_GITHUB = "your_github_token_here";

// Your Formspree form ID
window.env.FORMSPREE_ID = "your_formspree_id_here";

// Log loaded environment variables (without showing the actual values)
console.log('Local environment variables loaded:');
Object.keys(window.env).forEach(key => {
    console.log(`- ${key}`);
});
